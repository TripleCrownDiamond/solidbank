<div class="py-12">
    <div class="max-w-7xl mx-auto sm:px-6 lg:px-8">
        <div class="bg-white overflow-hidden shadow-xl sm:rounded-lg">
            <div class="p-6">
                <h1 class="text-3xl font-bold text-gray-900 mb-4">{{ __('Welcome to SolidBank') }}</h1>
                <p class="text-gray-600 mb-4">{{ __('Your trusted financial partner') }}</p>
                
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">
                    <div class="bg-blue-50 p-6 rounded-lg">
                        <h2 class="text-xl font-semibold text-blue-900 mb-2">{{ __('Secure Banking') }}</h2>
                        <p class="text-blue-700">{{ __('Your security is our top priority') }}</p>
                    </div>
                    
                    <div class="bg-green-50 p-6 rounded-lg">
                        <h2 class="text-xl font-semibold text-green-900 mb-2">{{ __('24/7 Support') }}</h2>
                        <p class="text-green-700">{{ __('We are here to help you anytime') }}</p>
                    </div>
                    
                    <div class="bg-purple-50 p-6 rounded-lg">
                        <h2 class="text-xl font-semibold text-purple-900 mb-2">{{ __('Easy Access') }}</h2>
                        <p class="text-purple-700">{{ __('Manage your finances with ease') }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
